import * as ottoBrowser from '../main';

// kickoff other server for testing...?

describe('standard use case', () => {
  let results

  beforeAll(() => {
    ottoBrowser.run();
  });


  // handle all actions

  // handle data type
});

describe('edge cases', () => {
  // errors (i.e. not checkable)
});
